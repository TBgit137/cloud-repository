var tag = document.getElementsByTagName(); //获取标签元素

var className = document.getElementsByClassName(); //获取类名元素

var name = document.getElementsByName(); //获取名称元素

var id = document.getElementById(); //获取id元素

var querySelector = document.querySelector(); //获取查询元素

var querySelectorAll = document.querySelectorAll(); //获取查询所有元素

var element = document.createElement(); //创建元素

var text = document.createTextNode(); //创建文本节点

var attribute = document.createAttribute(); //创建属性节点

