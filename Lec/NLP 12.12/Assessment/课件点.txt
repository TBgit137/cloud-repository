NLP Tasks：Segmentation Tokenisation token

CFG/PCFG，CNF，Probabilistic CKY Parsing，Parse Tree

N-gram Models, Add-one (Laplace) Smoothing, Interpolation, Stupid Backoff, Extrinsic (in-vivo) Evaluation, Intrinsic (in-vitro) Evaluation

Information Theory, entropy, Cross-entropy, (binary)Naive Bayes Classifier, Bag of Words, Confusion Matrix(画图), F1 Score

Generative Classifier(Naïve Bayes), Discriminative Classifier(Logistic Regression), Supervised Classification

Term-document Matrix(图), 文档向量 词向量(图)

TF-IDF, Word2Vec(CBOW预测器, Skip-Gram), GloVe

Recurrent Neural Networks, Long Short-Term Memory, 编码器-解码器架构, Attention Mechanism

Position Embedding, Generating Classification

BERT, Fine-tuning

gpt